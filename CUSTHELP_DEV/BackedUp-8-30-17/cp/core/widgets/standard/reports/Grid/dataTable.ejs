<table id="<%= tableID %>" class="yui3-datatable-table" role="grid">
    <caption><%= caption %></caption>
     <thead class="yui3-datatable-columns">
     <rn:block id="topHeader"/>
        <tr>
        <% for (var h=0; h < headers.length; h++) { %>
            <th class="yui3-datatable-header yui3-datatable-sortable-column" style="<%= headers[h].style %>"><%= headers[h].label %></th>
        <% } %>
        </tr>
     <rn:block id="bottomHeader"/>
    </thead>
    <tbody class="yui3-datatable-data">
    <rn:block id="topBody"/>
    <% for (var r=0; r < rows.length; r++) { %>
        <rn:block id="preBodyRow"/>
        <tr role="row" class="yui3-datatable-<%= (r % 2 === 0) ? 'even' : 'odd' %>">
        <% for (var d=0; d < rows[r].length; d++) { %>
            <td role="gridcell" class="yui3-datatable-cell"><%= rows[r][d] %></td>
        <% } %>
        </tr>
        <rn:block id="postBodyRow"/>
    <% } %>
    <rn:block id="bottomBody"/>
    </tbody>
</table>